/*
** EPITECH PROJECT, 2019
** compa
** File description:
** compar
*/

#include "my.h"

#define MIN_2(a, b)(a < b ? a : b)

#define MIN_3(a, b, c)

int comparison_function(int *str)
{
    int i = 0;

    int a = str[i - 1];
    int b = str[i - intlen(str)];
    int c = str[i - intlen(str) - 1];

}